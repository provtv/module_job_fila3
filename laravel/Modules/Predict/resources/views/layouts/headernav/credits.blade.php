@auth

    <livewire:headernav.credits :profile="$_profile"/>

    {{-- <div class="flex items-center px-2 space-x-2">
        @include('ui::svg.credit')
        <div class="text-sm font-semibold">{{ number_format($_profile->credits, 0) }}</div>
    </div> --}}
@endauth